# Root-level requirements voor Railway deploy
# Combineert deps van endpoint + frontend backend + extractor + classifier.

# --- endpoint (FastAPI) ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-multipart>=0.0.9

# --- extractor + classifier (PDF pipeline) ---
pymupdf>=1.24
pdfplumber>=0.11
openpyxl>=3.1
rapidfuzz>=3.6
pyyaml>=6.0
pandas>=2.2
