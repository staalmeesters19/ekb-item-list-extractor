# --- Customer-confidential data ---
# PDF drawings AND any extractor output derived from them are customer IP — never commit.
data/*.pdf
data/*.PDF
data/*.xlsx
data/*.XLSX
data/*.xltm
data/*.XLTM
data/*.xml
data/*.XML
data/*.csv
data/*.json

# EKB-internal reference docs at project root (work instructions, raw template)
# The bundled writer template lives at extractor/src/writers/templates/ — that one IS tracked.
/*.xltm
/*.doc
/*.docx

# ProCos artikeldata-export (confidential) — bundled into Railway via .railwayignore,
# but NEVER committed to git.
/ProCos-export*.xlsx
/ProCos-export*.XLSX

# New ProCos artikeldata (232k version, fase 2). Also confidential.
/procos_data/

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
build/
dist/
.venv/
venv/
env/
.eggs/

# --- Test / coverage caches ---
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/

# --- IDE / editor ---
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# --- Streamlit local state ---
.streamlit/secrets.toml
frontend/.streamlit/secrets.toml

# --- Local / scratch output ---
/out/
/tmp/
*.tmp
*.log
*.bak

# --- Claude Code local config ---
.claude/
